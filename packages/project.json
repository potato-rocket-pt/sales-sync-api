{
  "name": "shared",
  "root": "packages",
  "sourceRoot": "packages/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages"
      }
    },
    "shared.pack": {
      "executor": "nx:run-commands",
      "options": {
        "command": "make shared.pack"
      }
    }
  }
}
