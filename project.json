{
  "name": "api-infra",
  "root": ".",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "make build"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "make dev"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "make deploy"
      }
    }
  }
}
